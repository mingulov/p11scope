[package]
name = "p11scope"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
license = "MIT OR Apache-2.0"
repository = "https://github.com/mingulov/pkcs11-scope"
description = "Non-interposing PKCS#11 observer on eBPF uprobes"
publish = false

[lib]
name = "p11scope"
path = "src/lib.rs"

[features]
unsafe-unvalidated-metadata = []
# Internal-only diagnostic: attribute every published discovery skip to the
# site that raised it. Stderr only; the capture document is byte-identical
# either way (src/discovery/attribution.rs).
skip-attribution = []

[dependencies]
anyhow = "1"
aya = "=0.14.0"
cryptoki-sys = "0.5"
libc = "0.2"
p11scope-ebpf-common = { path = "crates/ebpf-common", features = ["user"] }
p11scope-manifest = { path = "crates/manifest", features = ["identify"] }
pkcs11-module = { git = "https://github.com/mingulov/pkcs11-proxy-ng", rev = "a2aab6cd67d21d140277a4584942e06c903f165b" }
pkcs11-proxy-ng-types = { git = "https://github.com/mingulov/pkcs11-proxy-ng", rev = "a2aab6cd67d21d140277a4584942e06c903f165b" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
signal-hook = { version = "0.4.4", default-features = false }

[dev-dependencies]
aya-obj = "=0.3.0"
p11scope-discover = { path = "crates/discover" }
tempfile = "3"

[workspace]
members = ["crates/discover", "crates/manifest", "crates/ebpf-common"]
