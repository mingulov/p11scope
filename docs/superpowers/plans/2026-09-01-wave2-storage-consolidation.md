# Wave 2 — Storage Consolidation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Follow the Agent execution protocol in `ROADMAP.md` §"Release program" — Task 0 re-verified and corrected the anchors on 2026-09-02 at merged `main` `c26371f`; recheck them again immediately before each task.

**Goal:** Enforce the owner's two-directory rule — everything durable lives in `/home/user/src/m/pkcs11-scope` (public) or `/home/user/src/m/p11scope-ws` (workspace) — by migrating referenced artifacts, repointing live references, and putting `p11scope-ws` under real git custody, without falsifying historical records or deleting any original.

**Architecture:** Copy-verify-repoint, never move-and-hope: originals in `~/.local/state/p11scope` (11 GB) and `~/p11scope-vm-bases` (1.6 GB) stay in place until the owner approves deletion. Every copy uses an absent-target/no-clobber state machine: exact existing copies are no-ops and any other existing destination stops, except the one explicitly defined security-scan subset completion. Historical documents keep their old absolute paths (they record where things were AT THE TIME); one relocation record maps old roots → new roots, and only *live navigation* (the evidence index) and *live scripts* are repointed. `p11scope-ws` git tracks text/metadata and adjacent typed manifests; large binaries are gitignored.

**Tech Stack:** bash/coreutils, rsync, git. No production Rust behavior changes; two existing Rust source-text contract tests move with their shell literals. The four cargo gates still run.

**Spec:** `docs/superpowers/specs/2026-09-01-release-requirements-and-goal.md` §4 (binding rules 1–5) and §6; `docs/superpowers/specs/2026-09-01-p11scope-release-prd.md` §9.6.

## Global Constraints

- Entry gate: Wave 1 merged to `main` (its Task 0 already rescued the Stage 3A3 trove, mirrored `findings.json`, made the initial `p11scope-ws` commit, and fenced `.superpowers/sdd/`). Every W2 step is idempotent against that: check-then-do, skip only exact verified copies.
- **Never delete or move an original.** Copy + verify only. Deletion of the old roots is a separate owner-gated decision recorded at closeout (Task 6).
- **Never rewrite history**: a tracked doc that *records* an old absolute path as evidence of a past run keeps it verbatim. Only live navigation and executable scripts are repointed.
- Do not commit large binaries into `p11scope-ws` git: `*.tar.zst`, `*.qcow2`, `*.img`, VM disks — gitignored and bound by tracked adjacent typed manifests instead.
- SSH private keys (`~/p11scope-vm-bases/id_ed25519`) are copied with mode 0600 into `p11scope-ws/vm-bases/`; **rotation is owner-gated and NOT performed here** — record it as an open item.
- Four canonical gates after every commit (`CLAUDE.md` §Checks).
- Branch `hardening/wave2-storage` off `main`; one commit per task; merge after review-to-zero.

---

### Task 0: Verified-anchor preflight correction

- [x] **Step 1:** Re-verify the W2 plan against merged W1 and the preserved shadow-review handoff. The 2026-09-02 baseline is 30 tracked files / 157 matching reference lines, not the historical 27-file estimate; `/home` has 34,878,877,696 bytes free and still passes the unchanged ≥15 GiB gate; `p11scope-ws` has matching git identity and unrelated untracked custody material that must be classified, not blanket-ignored or deleted.
- [x] **Step 2:** Ratify the corrections now embedded below: no-clobber/partial-destination state machine; adjacent non-self-referential typed manifests; complete VM-tree inventory; a missing live target blocks exit unless its pointer is owner-reviewed and removed; Lane 13 gets an explicit destination; both runnable VM harnesses are repointed off `/tmp`; the closed W1 plan stays historical; memory remains explicit-request-only.

---

### Task 1: Inventory and classification manifest

**Files:**
- Create: `/home/user/src/m/p11scope-ws/preserved/<execution date>-storage-relocation/INVENTORY.md` (dates in this plan written `<execution date>` are filled at execution)

- [x] **Step 0 (preflight):** `df -B1 /home` — require ≥15 GiB free before every copy phase (copy-don't-move needs headroom for up to ~12.6 GB of duplicates); if short, STOP and ask the owner which originals to migrate first. Set `p11scope-ws` git identity from the public repo's config. Inventory all Git-visible private entries before changing ignores or staging; never use `git add -A`.
- [x] **Step 1:** Enumerate the actual merged-tree references once: `git grep -n -e '\.local/state' -e 'p11scope-vm-bases' -e 'pkcs11-scope-evidence' -e '/tmp/p11scope' -e 'home/user' -- .` (2026-09-02 baseline at `main@c26371f`: 30 files / 157 lines; classify the live result, never assert a pinned count). Classify each hit **by content, not filename**, into: **(a) live script** (`scripts/matrix/verify-oracle.sh`; `spike/slice1b2-loader/run-lanes.sh`; and the retained-input paths in `spike/slice1b2-{kernel,loader-host}/run.sh` plus their source-text tests), **(b) live navigation/authority pointer to a migrated artifact** — repoint or annotate (the five evidence-index items and `ROADMAP.md:215`'s Lane 13 facts pointer), **(c) historical record** — keep verbatim, including the closed W1 plan, **(d) genuinely ephemeral output or test-fixture literal** — document, don't change (`scripts/verify-task4-lane16.sh`'s negative fixture and runtime scratch outputs, but not retained VM inputs), **(e) already compliant / states the rule** — nothing to do. Every hit lands in exactly one class; a hit fitting none goes to INVENTORY.md as OPEN with a proposed class for review.
- [x] **Step 2:** Enumerate the storage roots (names, sizes, dates — no content dumps): `~/.local/state/p11scope/*` (lane02 evidence dirs, mvp-semantic attempt dirs, mvp-candidate freezes, `security-scan-3e10be9/`, `retired-generated-slice1b2-finish/`, `pkcs11-scope-portable-{90a03ac,3d3ba05,b86d4d5}.tar.zst`, **and `fedora44-base` — a VM base living in the wrong root; it goes to `p11scope-ws/vm-bases/`, NOT `preserved/`**), `~/p11scope-vm-bases/*` (`jammy`, `noble`, `logs`, `fix1-20260817`, `id_ed25519{,.pub}`). **REFERENCED means:** named by any tracked live file in the merged tree or by the evidence index. A missing referenced original makes the wave NON-PASS until its live pointer is removed **and** the item is reclassified with owner-reviewed truth text; record the missing state, never silently count it as migrated.
- [x] **Step 3:** Write `INVENTORY.md` with the classification, sizes, and the migration decision per item (migrate / leave-for-owner-deletion / ephemeral). Commit it in `p11scope-ws`.

### Task 2: Migrate referenced durable artifacts into `p11scope-ws`

- [x] **Step 0:** Before copying, ensure `p11scope-ws/.gitignore` covers large trees: `incoming/`, `vm-bases/`, `preserved/evidence-roots/`, `preserved/portable/`, and binary extensions. Keep the relocation `manifests/` tracked. Classify the existing 3,240 Git-visible private entries; preserve the verified `evidence/slice1b2/MANIFEST.sha256` and selected text metadata, add only narrow binary-evidence ignores or explicit add allowlists, and never use `git add -A`. If a large tree was ever committed, stop before history surgery. Commit only reviewed text/metadata/manifests.
- [x] **Step 1:** For every REFERENCED item, use this state machine: inventory source before copy; absent destination → copy to a same-parent sibling stage, re-inventory the source, verify exact typed equality, publish no-replace; exact existing equality → no-op; any partial, different, or unverifiable destination → STOP for explicit merge adjudication. Interrupted reruns re-enter the same state machine; never copy into an existing destination. The sole pre-adjudicated exception is `security-scan-3e10be9`: verify the existing destination's matching `findings.json` plus its custody manifest; stage all eight source files; publish only source entries absent from that existing destination with per-file no-clobber checks; reject every collision; then require the exact final set of eight source files plus the existing custody manifest. Fixed destinations: the Task 4 SDD trove → `preserved/sdd/2026-08-27-task4-receipt-closure/` (the W1 Task 0 exact copy must take the no-op branch); security scan → `preserved/security-scan-3e10be9/`; portable archives/checksums → `preserved/portable/`; referenced semantic/candidate evidence roots → `preserved/evidence-roots/<original-name>/`; Lane 13 `facts.log` → `preserved/evidence-roots/task4-lane13-a2fd9ee-20260826T2135EEST/facts.log` (mode 0600, SHA-256 `b96cbed6cbc2963dab2c5963b5c52f6378d9bef313479b83a56c259df79b94f3`). Unmapped items remain OPEN; `retired-generated-slice1b2-finish/` is not blanket-copied. The Fedora VM base is part of Step 3's single combined VM transaction, not copied here.
- [x] **Step 2:** For every source/destination, compare an exact non-self-referential typed inventory using no-dereference metadata: relative path, type, mode, regular-file size/hash, and raw symlink target. Reject hardlinks, dangling/external symlinks, special files, or unsupported metadata unless separately adjudicated. Store canonical manifests under tracked `p11scope-ws/preserved/<execution date>-storage-relocation/manifests/`, outside ignored destination trees.
- [x] **Step 3:** Publish `p11scope-ws/vm-bases/` once as a combined absent-target transaction: stage the complete `~/p11scope-vm-bases/` tree plus `~/.local/state/p11scope/fedora44-base/` at leaf `fedora44-base/`, then verify exact typed equality against both source components before no-replace publication. The legacy VM source baseline is 12 directories, 127 regular files, and 1,714,727,520 apparent bytes. Preserve every descendant source mode; require only `id_ed25519` to be 0600 (`id_ed25519.pub` remains 0444), while the new top-level private custody root and `vm-bases/` are 0700.

### Task 3: Repoint live references

**Files:**
- Modify: `scripts/matrix/verify-oracle.sh`; `spike/slice1b2-kernel/run.sh` and `spike/slice1b2-kernel/src/main.rs`; `spike/slice1b2-loader-host/run.sh` and `spike/slice1b2-loader-host/src/main.rs`; `docs/superpowers/reports/2026-08-31-productization-evidence-index.md`; `docs/superpowers/plans/ROADMAP.md`

- [x] **Step 1:** `verify-oracle.sh:42` → `PKCS11_CHECK_DIR=${PKCS11_CHECK_DIR:-$HOME/src/m/pkcs11-check-ws/pkcs11-check}` (env-overridable). **Two flags for the record, not for silent settling:** (i) `pkcs11-check-ws` is a third directory — it is a sibling *project checkout*, not p11scope data, but under a literal reading of spec §4 rule 1 this is an exception the owner must ratify (Task 4 records it as OWNER-PENDING). (ii) The script's provenance ledger (`:160-177`, `git -C "$PKCS11_CHECK_DIR" rev-parse HEAD` + `pip freeze`) binds the receipt to whatever checkout the env names — that is correct (content-bound, not path-bound), but note that pre-existing lane-11 receipts pin the OLD script hash, so a rerun is needed; the `sh -n` gate and self-test string pin currently live around `tests/artifact_contracts.rs:4120-4130,4343-4344` and move with the edit.
- [x] **Step 2:** Repoint the five evidence-index items and `ROADMAP.md:215` to the exact migrated destinations, appending "(originally under `~/.local/state/p11scope/`, relocated <execution date>)". Do not edit the closed W1 plan: its present private findings pointer is already compliant and the plan is historical.
- [x] **Step 3:** In both runnable VM harnesses, add one explicit `P11SCOPE_VM_BASES=${P11SCOPE_VM_BASES:-$HOME/src/m/p11scope-ws/vm-bases}` default and derive retained overlays, official images, serial logs, and `id_ed25519` from it; update the existing source-text tests. Runtime scratch outputs may remain under private temporary run roots.
- [x] **Step 4:** Symlink decision (spec §4.5): KEEP both shims — `/home/user/src/m/pkcs11-scope-evidence` → `p11scope-ws/evidence` and `p11scope-ws/source` → the repo. They already comply with the two-directory rule and `run-lanes.sh` resolves through the first. Record the decision in the relocation record.
- [x] **Step 5:** Full gates; commit `chore: repoint live references to the two-directory layout`.

### Task 4: Relocation record

- [x] **Step 1:** Write `docs/superpowers/reports/<execution date>-storage-relocation.md` (public, small): the two-directory rule, the old-root → new-root map, the keep-both-symlinks decision, the historical-docs-keep-old-paths policy (the working interpretation of spec §6's storage bullet — surfaced for owner ratification, PRD §9.6 note), the `pkcs11-check-ws` third-directory exception (OWNER-PENDING), a pointer to `p11scope-ws/preserved/.../INVENTORY.md`, and the open owner-gated items (delete old roots; rotate VM SSH keys).
- [x] **Step 2:** Gates; commit.

### Task 5: Custody verification

- [x] **Step 1:** Assert: `p11scope-ws` has ≥1 commit and no staged/tracked modification after the migration commits. Every remaining Git-visible entry must be tracked, narrowly ignored, or recorded with an explicit custody decision; blanket ignores/deletion are forbidden. Record `git -C p11scope-ws log --oneline | head` and the classified status.
- [x] **Step 2:** Assert no tracked file's *executable or live-navigation* path resolves into `~/.local/state`, `~/p11scope-vm-bases`, or `/tmp` (re-run the Task 1 grep; every remaining hit must be classified (c) historical, (d) ephemeral/test-only, or (e) already compliant/rule text, plus class (a) only where the live path resolves through an approved shim whose endpoint is inside the two roots — zero unclassified).
- [x] **Step 3:** Spot-restore test: pick one migrated evidence file, verify its sha256 matches both the manifest and the original.

### Task 6: Review-to-zero and closeout

- [x] **Step 1:** Two independent review agents per the ROADMAP protocol: (a) Sol xhigh rule-compliance review (does anything durable still depend on a non-durable root? was any history rewritten?), (b) Luna high custody review (manifests complete, perms right, nothing large in ws git). Add Terra xhigh only for a distinct third lane when materially useful. Triage, fix, repeat until a cycle accepts zero findings. Sol xhigh and Luna high accepted zero findings at public tip `f2d5979` and private tip `18b0c7a`.
- [x] **Step 2:** Merge to `main`; gates on `main`. Local merge `e5e8517` passed all four canonical gates with 937 tests and zero failures.
- [x] **Step 3:** Present the owner-gated deletion list (old roots with sizes: `~/.local/state/p11scope` ~11 GB, `~/p11scope-vm-bases` ~1.6 GB) and the key-rotation item. **Stop — do not delete or rotate.** The relocation report records the exact current sizes; neither root was deleted and the key was not rotated.
- [x] **Step 4:** Memory update is conditional on an explicit contemporaneous owner request; otherwise record `SKIPPED`. If requested, record that the two-directory rule is enforced as of `<tip>`, originals await owner deletion, and keys await rotation. **SKIPPED:** no contemporaneous owner request.
