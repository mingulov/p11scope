//! p11scope — non-interposing PKCS#11 observer (eBPF uprobes).
//! Status: design phase; see docs/superpowers/specs/.

fn main() {
    eprintln!("p11scope 0.0.0-dev — design phase, no functionality yet");
    std::process::exit(1);
}
